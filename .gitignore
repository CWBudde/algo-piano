# Binaries
bin/
piano-render
piano-fit-fast
piano-fit
!piano-fit/
!piano-fit-fast/
!piano-render/
/piano-wasm
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Test output
*.wav
!assets/ir/*.wav
*-test.wav
output.wav
bench-*.wav
tests/*.wav
# Everything under out/ is tool output: fit work dirs, stage and pass presets,
# rendered candidates and their reports.
out/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Go
vendor/
internal/cpu/cpu.go
internal/bytealg/bytealg.go

# OS
.DS_Store
Thumbs.db

# Project-specific
assets/ir/*.wav.old
assets/presets/*.json.old
assets/presets/fitted-*.json
assets/presets/*.report.json
# keep IRs themselves as they are assets (or ignore if they are huge and managed via LFS/externals)
# assets/ir/

# Profiling data
*.prof
cpu.prof
mem.prof

# Web demo build artifacts
web/dist/
web/wasm_exec.js

# Agent worktrees (never belong in the repo)
.claude/worktrees/
